apiVersion: v1
kind: Service
metadata:
  name: {{ include "newapi.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "newapi.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http